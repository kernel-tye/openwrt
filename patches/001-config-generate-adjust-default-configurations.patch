--- a/package/base-files/files/bin/config_generate	2022-03-08 11:05:10.483021442 +0800
+++ b/package/base-files/files/bin/config_generate	2022-03-08 11:22:19.145159428 +0800
@@ -45,13 +45,16 @@
 		set network.loopback.ipaddr='127.0.0.1'
 		set network.loopback.netmask='255.0.0.0'
 	EOF
-		[ -e /proc/sys/net/ipv6 ] && {
-			uci -q batch <<-EOF
-				delete network.globals
-				set network.globals='globals'
-				set network.globals.ula_prefix='auto'
-			EOF
-		}
+	uci -q batch <<-EOF
+		delete network.globals
+		set network.globals='globals'
+		set network.globals.packet_steering='1'
+	EOF
+	[ -e /proc/sys/net/ipv6 ] && {
+		uci -q batch <<-EOF
+			set network.globals.ula_prefix='auto'
+		EOF
+	}
 
 	if json_is_a dsl object; then
 		json_select dsl
@@ -114,6 +117,7 @@
 			add network device
 			set network.@device[-1].name='br-$1'
 			set network.@device[-1].type='bridge'
+			set network.@device[-1].stp='1'
 		EOF
 		for port in $ports; do uci add_list network.@device[-1].ports="$port"; done
 		[ -n "$macaddr" ] && {
@@ -302,7 +306,8 @@
 		delete system.@system[0]
 		add system system
 		set system.@system[-1].hostname='OpenWrt'
-		set system.@system[-1].timezone='UTC'
+		set system.@system[-1].timezone='CST-8'
+		set system.@system[-1].zonename='Asia/Shanghai'
 		set system.@system[-1].ttylogin='0'
 		set system.@system[-1].log_size='64'
 		set system.@system[-1].urandom_seed='0'
@@ -311,10 +316,11 @@
 		set system.ntp='timeserver'
 		set system.ntp.enabled='1'
 		set system.ntp.enable_server='0'
-		add_list system.ntp.server='0.openwrt.pool.ntp.org'
-		add_list system.ntp.server='1.openwrt.pool.ntp.org'
-		add_list system.ntp.server='2.openwrt.pool.ntp.org'
-		add_list system.ntp.server='3.openwrt.pool.ntp.org'
+		add_list system.ntp.server='ntp.ntsc.ac.cn'
+		add_list system.ntp.server='edu.ntp.org.cn'
+		add_list system.ntp.server='ntp.aliyun.com'
+		add_list system.ntp.server='pool.ntp.org'
+		add_list system.ntp.server='time.nist.gov'
 	EOF
 
 	if json_is_a system object; then
